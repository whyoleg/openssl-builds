name: Run build
on: [ workflow_call, workflow_dispatch ]

jobs:
  openssl3:
    name: Build openssl3
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ windows-latest ]
        target: [ mingw-x64, windows-x64 ]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install conan

      - run: conan install packages/openssl3 --install-folder build/openssl3/${{ matrix.target }} --build=missing -pr:b default -pr:h profiles/${{ matrix.target }} -o openssl:shared=True
      - run: conan install packages/openssl3 --install-folder build/openssl3/${{ matrix.target }} --build=missing -pr:b default -pr:h profiles/${{ matrix.target }} -o openssl:shared=False

      - uses: actions/upload-artifact@v3
        if: always()
        with:
          name: openssl3-all
          if-no-files-found: error
          path: |
            build/openssl3/*/lib/*
            build/openssl3/*/include/openssl/*

#      - uses: actions/upload-artifact@v3
#        if: always()
#        with:
#          name: openssl3-${{ matrix.target }}
#          if-no-files-found: error
#          path: |
#            build/openssl3/${{ matrix.target }}/lib/dynamic/*
#            build/openssl3/${{ matrix.target }}/lib/static/*
#            build/openssl3/${{ matrix.target }}/include/openssl/*
